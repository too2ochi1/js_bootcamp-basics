// Global (name)
  // Local (name)
    // Local
  // Local
 
 let name = 'Jason'
 
if (true) {
    let name = 'Mike'

    if (true) {
        name = 'Jasper'
        console.log(name)
    }
}

if (true) {
    console.log(name) 
}